animation_duration_ms 250

corner_radius 8

shadows enable
shadow_blur_radius 12
shadow_color #00000055

# Blur is the most likely effect to glitch or drop frames on the hybrid-NVIDIA
# path this session launches with (--unsupported-gpu). Enable only after the
# above is confirmed stable.
# blur enable
# blur_passes 2
# blur_radius 5
